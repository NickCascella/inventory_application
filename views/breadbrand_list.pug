extends layout

block content
  div
    h1= title
    div(class='breads-list')
      ul(class='items-list')
        each brand in breadbrand_list
          li
            div(class='items-list-img-title')
              img(src=`/images/${brand.img}` class='logo-mini' )
              a(href=brand.url class='link') #{brand.title}
            p #{brand.description}
            

        else
          li We carry no bread.