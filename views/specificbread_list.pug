extends layout

block content
  h1= title

  ul
    each bread in specificbreads_list
      li
        a(href=bread.url) #{bread.brand.title} #{bread.type} 
        |  #{bread.details}

    else
      li There are no brands of bread :(